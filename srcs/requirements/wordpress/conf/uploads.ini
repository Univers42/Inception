; Matches nginx client_max_body_size (64M) so large media uploads
; are not silently rejected by PHP after passing nginx.
upload_max_filesize = 64M
post_max_size       = 64M
memory_limit        = 256M
max_execution_time  = 300
